# Dependencies
node_modules/
package-lock.json
yarn.lock

# Build outputs
dist/
.next/
out/
build/

# Development — secrets (use repo root `.env` / `.env.local` or `web/.env` or per-app `.env.local`)
.env
.env.local
.env.*.local
web/.env
packages/apps/**/.env.local
.DS_Store

# IDE
.vscode/
.idea/
*.swp
*.swo

# TypeScript incremental
**/tsconfig.tsbuildinfo

# Turbo
.turbo/
.plans/

# OS
Thumbs.db

# Legacy/Project specific
.clasprc.json
.claude
*.xlsx
gas/.deployment-id

# Local DB dumps (pg_dump / Neon exports — can include \\restrict secrets; not source of truth)
/neon-schema.sql
**/neon-schema.sql
