---
description: Google OAuth env vars — monorepo Next apps (fintracker, vault, staff)
alwaysApply: true
---

# Google OAuth environment (shared convention)

All Next apps under `packages/apps/*` use **`packages/apps/resolve-google-env.cjs`** via `getGoogleAuthEnv(__dirname)` in `next.config.js`.

## Client ID — use any one of these names (same value)

1. `VITE_GOOGLE_CLIENT_ID` (shared with Vite / `web/`)
2. `NEXT_PUBLIC_GOOGLE_CLIENT_ID`
3. `GOOGLE_CLIENT_ID`

**Local file merge** (later files override earlier keys; only fills `process.env` keys that are still empty):  
repo **`.env`** → repo **`.env.local`** → **`web/.env`** → **`packages/apps/<app>/.env.local`**.

Vercel / shell env wins over all files.

`next.config` mirrors a resolved id into `NEXT_PUBLIC_GOOGLE_CLIENT_ID` and `VITE_GOOGLE_CLIENT_ID` for the client bundle.

## Allowlist (optional)

`VITE_ALLOWED_EMAILS` or `NEXT_PUBLIC_ALLOWED_EMAILS` or `ALLOWED_EMAILS` (comma-separated).

## Do not

- Add app-specific Google env key names — keep the three client-id keys above.
- Skip `getGoogleAuthEnv` in new Next apps; copy `next.config.js` env block from fintracker/vault/staff.

## If the id is “missing” locally

Set `VITE_GOOGLE_CLIENT_ID=...` in **repo** `.env` or `.env.local`, or `web/.env`, or `packages/apps/<app>/.env.local`, then restart `next dev` (or `dev:*:fresh`).

**GAS URL** for `/api/gas-proxy` uses the same merged files for `VITE_GAS_URL` / `NEXT_PUBLIC_GAS_URL` when not in `process.env`.

## Server auth (iron-session + GAS proxy)

- **`SESSION_SECRET`** — 32+ character random string; encrypts the HttpOnly session cookie (`ft_session_fintracker` / `ft_session_vault` / `ft_session_staff` per app). Required for login + gated `/api/gas-proxy` once set (middleware and API routes fail open if unset during rollout).
- **`APP_PASSWORD`** — 4-digit PIN validated only on the server (`POST /api/auth/verify-pin`). Prefer this over client-exposed `VITE_APP_PASSWORD` / `NEXT_PUBLIC_APP_PASSWORD` (removed from the client bundle). **`PIN_SESSION_EMAIL`** (optional) — if there is no Google session cookie but the PIN is correct, the server binds the session to this email; otherwise the first entry in **`ALLOWED_EMAILS`** / **`VITE_ALLOWED_EMAILS`** is used for PIN-only session bootstrap.
- **`VITE_API_TOKEN`** — GAS API token; **server-only** for proxy injection. Do **not** put it in `NEXT_PUBLIC_*` (removed from `next.config` client env).

Allowlist for Google sign-in remains **`VITE_ALLOWED_EMAILS`** / **`NEXT_PUBLIC_ALLOWED_EMAILS`** / **`ALLOWED_EMAILS`** (checked in `POST /api/auth/google`, not exposed on the client).
