# Dev: Admin (fresh)

Start Admin on port 3003 with ports cleared and cache reset.

```bash
pnpm dev:admin:fresh
```

For DB/login issues on admin, also run:

```bash
pnpm db:check:admin
```

Reference: `CLAUDE.md`
