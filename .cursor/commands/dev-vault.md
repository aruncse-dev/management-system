# Dev: Vault (fresh)

Start Vault on port 3001 with ports cleared and cache reset.

```bash
pnpm dev:vault:fresh
```

After editing shared UI or auth packages, run `/build-ui-auth` first.

Reference: `CLAUDE.md`
