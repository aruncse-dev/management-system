# Build: Fintracker (production)

Production build for Fintracker only.

```bash
pnpm build:fintracker
```

Run `pnpm prepush` first when validating before release. For all apps: `pnpm build`.

Reference: `CLAUDE.md`
