{
  "installCommand": "cd ../.. && corepack enable pnpm && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm build --filter '@fintracker-vault/*' && cd packages/apps/vault && next build",
  "outputDirectory": ".next"
}
