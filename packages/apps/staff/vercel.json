{
  "installCommand": "cd ../../.. && corepack enable pnpm && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../../.. && pnpm build --filter '@fintracker-vault/*' && pnpm --filter staff run build",
  "outputDirectory": ".next",
  "ignoreCommand": "exit 1"
}
