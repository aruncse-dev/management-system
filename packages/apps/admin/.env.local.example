# Copy to .env.local — Admin uses the same Neon DB as FinTracker.

SESSION_SECRET=
DATABASE_URL=

# Platform encryption for sensitive DB columns (*_enc). See docs/sensitive-field-encryption.md
# Generate: openssl rand -base64 32
# FIELD_ENCRYPTION_KEY=
# FIELD_ENCRYPTION_KEY_ID=1
